<%= form_with(model: plan) do |form| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
        <% plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :has_exports %>
    <%= form.check_box :has_exports %>
  </div>

  <div class="field">
    <%= form.label :has_breakdown %>
    <%= form.check_box :has_breakdown %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :has_csv_import %>
    <%= form.check_box :has_csv_import %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
