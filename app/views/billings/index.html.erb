<p id="notice"><%= notice %></p>

<h1>Billings</h1>

<table>
  <thead>
    <tr>
      <th>App</th>
      <th>User</th>
      <th>Business</th>
      <th>Amount</th>
      <th>Plan</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @billings.each do |billing| %>
      <tr>
        <td><%= billing.app_id %></td>
        <td><%= billing.user_id %></td>
        <td><%= billing.business_id %></td>
        <td><%= billing.amount %></td>
        <td><%= billing.plan_id %></td>
        <td><%= link_to 'Show', bill_path(billing.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
